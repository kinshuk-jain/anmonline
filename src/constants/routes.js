const SERVICE_METADATA = '/service-metadata'
const LOGIN = '/login'
const REFRESH_TOKEN = '/refresh-token'
const LOGOUT = '/logout'
const CREATE_USER = '/create-user'
const GET_NAMES = '/get-names'
const LOAD_USER = '/load-user'

module.exports = {
  SERVICE_METADATA,
  LOGIN,
  REFRESH_TOKEN,
  LOGOUT,
  CREATE_USER,
  GET_NAMES,
  LOAD_USER
}
